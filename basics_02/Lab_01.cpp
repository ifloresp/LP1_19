int area(int length, int width);

int main(){
	int s1=area(7;
	int s1=area(7)
	Int s3=area(7);
	int s4=area('7);
	return area(4,4);
}
int area(int length, int width)
{
	return length*width;
}
